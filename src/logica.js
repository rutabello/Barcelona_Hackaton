// in App : return <GeneralSquare component />
// GeneralSquare : return a map (of GenralSquare.length) of smallSquares
// smallSquarecomponent : state : true / false

// aPP : counter
//generalSquare = Array of small squares number
// every 1sc/0.5sc/0.25sc, a small square appear. appearing small square = random(generalSquare)
// > the smallSquare randomes > its state changes from false (default) to true

// the previous false disappear when time is over

//dans SmallSquare component : Onclick :
// SmallSquare.state.display = false ? return counter : return counter + 1 and it changes back to false

// everytime


// put the pointer as a hand
// change your team >> the name of your team will be returned in the announcement of the result
// choose the team you want to destroy >> the name will appear in the announcement of the result